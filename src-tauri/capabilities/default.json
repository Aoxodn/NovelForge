{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "NovelForge 主窗口默认能力",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:window:allow-destroy"
  ]
}
