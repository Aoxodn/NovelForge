# NovelForge 更新日志（CHANGELOG）

## [1.0.1-beta.1] - 2026-09-08

### 修复

- **人物关系层 L1/L2 隔离**：修复在卷画布（L1）创建的人物关系会渗透到卷内画布（L2）的问题。`list_character_relations` 改为严格按 scope 隔离：无参仅返回跨卷关系（L1），传卷 id 仅返回该卷关系（L2）。新增 `list_all_character_relations` 供人物卡管理视图使用。
- **L2 关系刷新**：修复卷内创建/编辑关系后刷新调用未传 volumeId 导致拉取全局关系的问题。

### 改进

- **关系作用范围选择器**：L1 仅可选"跨卷（全书）"，L2 仅可选"仅本卷"，两层职责清晰，避免误建跨层关系。
- **单字人名误判排除**（V13）：单字人名自动填入内置误判词词典（90+ 常见单字，含中文姓和音译外来名），统计时先剔除"简单""简历"等误判词再计数；人物卡中可自行增删排除词。

---

## [1.0.0-beta.1] - 2026-09-08

首个 1.0 Beta 版本。经过 0.1~0.9 共 19 个迭代版本，核心功能闭环，进入 Beta 阶段。

### 核心特色：可视化写小说

- **分形画布三级下钻**：L1 全书图谱（卷节点 + 剧情线 + 伏笔）→ L2 卷内画布（章节节点 + 人物节点 + 章间连线 + 小节分组）→ L3 章节编辑器，同一套渲染引擎递归使用
- **故事地图（L1）**：卷作为节点，连线标记顺序/因果/分支/汇合/伏笔回收/平行叙事/闪回七种边型；剧情线分泳道分层布局；伏笔可锚定具体章节、流转状态（活跃/已回收/失效）；自动布局按主线居中、支线浮上、暗线沉下
- **卷内画布（L2）**：章节节点自由拖动落库，章间连线可拖弯，小节（章节群）组框整体拖动，小节间汇总弧；布局策略可切换（线性分支/辐射/力导向/小节分组）
- **人物图谱层**：人物节点上画布（L1 跨卷轨迹带 + L2 卷内出场堆叠），可自由拖动落库；拉线建关系（五大类 25+ 子类型，单双向，跨卷/指定卷范围）；人物视角模式高亮该人物出场与关联

### 写作与大纲

- **三级大纲**：全书大纲 → 卷细纲 → 章纲/作者笔记，600ms 防抖自动保存
- **章节管理**：新建/删除/回收站/拖拽排序/状态标记/字数统计
- **全书总览**：剧情线进度、伏笔追踪、人物出场热度、断档预警
- **写作界面右栏**：本章发展图 + 出场人物 + 关联伏笔

### 人物 / 地点卡

- **手动建卡 + 精确匹配统计**：名字+别名精确匹配，确定性零误判
- **单字名误判排除**（V13）：单字人名自动填入内置误判词词典（90+ 常见单字，含中文姓和音译外来名），统计时先剔除"简单""简历"等误判词再计数；作者可在人物卡中自行增删排除词
- **人物关系体系**：血缘/情感/社会/阵营/叙事五大类，自定义子类型，画布上拉线建关系
- **手动绑定**：人物可手动关联到卷/章节（正文没提及也能挂）

### 智能工具

- **命名生成器**：8 类名词（人名/地名/势力/功法/装备/丹药/动物/灵植），25+ 题材覆盖，核心前缀≥100 后缀≥50
- **智能导入**：TXT/DOCX 自动拆章，按卷结构导入
- **导出**：多格式导出

### 技术与体验

- **本地优先**：Tauri 2 + React 19 + Rust + SQLite，数据全在本地，完全离线
- **无边框窗口 + 三主题**：浅色/深色/护眼，全局数据库 `%APPDATA%/com.novelforge.app/`
- **Obsidian 式关系图谱动态效果**：弹簧锚定漂移 + 光晕层 + hover 邻接高亮
- **边路由类型分道**：同对节点多边按类型分泳道，标签嵌线中点断开，箭头头部按泳道角度错开不重叠

### 已知限制（Beta 阶段）

- 分形画布 L3（章内场景级）尚未实现
- 自动布局在超大规模（50+ 卷）下性能待优化
- 部分题材命名生成器词库仍在扩充

---

## [0.9.19] - 2026-09-08

### 修复 / 新增

- **修复人物关系边箭头头部重叠**：同对角色节点之间多条关系边的箭头此前全部扎在目标圆同一点上，导致箭头头部重叠。新增 `circleEdgeEndpoints()`——两端点在各自圆上按泳道角度同向旋转（每泳道单位 ~26°），两条边终点差 ~31°，箭头充分分离；L1 / L2 人物关系边统一使用
- **角色节点右键菜单增加「编辑人物」「删除人物」**：L1 全书图谱和 L2 卷内画布的角色节点右键均可直接编辑人物信息（名字/别名/角色定位/备注）或删除人物（含二次确认，级联清除关系与提及记录）；L2 保留原有的「查看详情」「移出本卷」
- 顺带修复 L1 人物边偏移量计算错误：原代码直接将 lane 系数（±0.6）当作像素传入，实际偏移几乎不可见，现通过 `edgeOffsetPx` 正确计算

## [0.9.18] - 2026-09-08

### 修复 / 改进

- **L1 / L2 画布稳定性修复**：故事地图与卷内画布在大量节点（30+ 章节 + 人物节点）下的渲染性能与交互响应优化；节点拖动、连线命中、缩放平移在高密度画布下不再卡顿或错位
- **人物节点布局修正**：跨卷人物节点与卷内人物节点的自动定位算法调优——未手动拖动过的人物节点不再与章节节点重叠，围绕出场最多章节的堆叠间距更合理；手动拖动过的位置继续优先落库
- **连线标签嵌线兼容性**：标签中点断开（字即线）在曲线弧度较大（手动拖弯 / 长跨度伏笔）时不再出现文字与线段错位或缺口偏移；缩放时缺口宽度随曲线长度同步补偿
- **卷内画布返回状态恢复**：从 L2 卷内画布返回 L1 全书图谱时，L1 的平移/缩放/选中状态完整恢复，不再重置到自适应视图
- **小节（章节群）组框渲染修复**：组成员章节被删除或移出后，组框自动重新包络，不再出现空组框或尺寸不更新；两小节之间的汇总弧在成员连线全部删除后自动消失

## \[0.9.17] - 2026-09-08

### 改进（连线标签与超量）

- **标签嵌线（中点断开）**：边标签回到连线正中，文字所在的线段精确断开——字即线、线即字，视觉连续不再悬浮在线旁；按文字实际宽度（CJK 全宽 / 拉丁半宽）在曲线上挖缺口，缺口随缩放补偿同步变化；剧情线徽标移到 t=0.2、拖弯手柄移到 t=0.75，避开中点文字
- **同对节点连线上限 4 → 6**：第五、六条线不再被合并进「+N」徽标，全部直接渲染（超出 6 条才合并）

## \[0.9.16] - 2026-09-08

### 新增（可编辑人物层：主动人物图谱，V12 迁移）

- 人物层从「被动展示」升级为「主动编辑」——人物节点可自由拖动落库（L1 全书图谱坐标 `characters.map_x/map_y`，L2 卷内坐标 `char_volume_pos` 每卷独立），拖到哪写到哪，自动轨迹带定位仅在未拖动过时生效
- **画布上拉线建关系**：人物节点悬停浮现拉线圆点——拖到另一人物上松手即弹「建立人物关系」（五大类 / 子类型 / 单双向 / 说明 / 作用范围：跨卷或指定卷），关系边点击可编辑 / 删除（L1 / L2 同款）
- **手动关联卷与章**：L1 拉线拖到卷卡上 = 人物关联该卷（细虚线，正文没提及也能挂）；L2 拉线拖到章节上 = 人物关联该章；右键人物可解除绑定
- **自由添加人物上画布**：右键空白「添加人物到图谱 / 到本卷」→ 从人物卡中挑选，即使正文零提及也能作为纯图谱节点常驻，自由摆放拖出关系网
- 修复：L2 画布连线命中层被父级 `pointer-events:none` 连累、点击编辑此前无效的问题

## \[0.9.15] - 2026-09-08

### 修复 / 改进

- **修复平行叙事 / 闪回连线整条隐形**：连线的颜色由 CSS 类（et-0~et-4）定义，新增的边型 5/6 没有对应规则导致 stroke 为默认 none——补齐三主题语义色（平行=青色实线加粗、闪回=橙色长虚线）与标签配色，图例与画布终于一致
- **修复暗色主题下原生下拉面板看不清字**：`color-scheme` 跟随主题 + 显式设置 `<option>` 的背景/文字色，「角色定位」等所有下拉在黑色背景下可读
- **线上文字随缩放反向补偿**：缩小画布时标签字号相对加大（更易读），放大时相对收敛（更纤细），白底描边同步缩放（L1/L2 全部边标签覆盖）
- **布局切换焦点追踪**：切换布局策略 / 自动布局后视图自动跟随（选中章优先居中，否则自适应全图）
- **布局模式重做**：辐射改为真圆形环绕；力导向加强（连线吸引 + 中心引力，结果确定）

## \[0.9.14] - 2026-09-08

### 新增（L2 卷内画布可编辑化）

- **L2 卷内画布可编辑化**（与 L1 同一套交互语言，V10 迁移）：
  - 章节节点自由拖动，松手落库（`chapters.map_x / map_y`，重启不丢）；双击章节直接打开正文
  - **章间连线**：节点右侧圆点拖出 → 建边（顺序/因果/分支/汇合/伏笔/平行/闪回，伏笔必填内容、可流转状态），新表 `chapter_edges`（仅限同卷）；边可拖弯（中点手柄落库 `bend`）、点击编辑 / 删除；相邻章自动顺序线（该对已有手工边时自动避让，可开关）
  - **小节（章节群）**：新表 `chapter_groups` + `chapters.group_id`；组框自动包络成员、整体拖动；右键组框 重命名 / 纵向对齐成员 / 删除（章节保留）；右键章节 加入 / 移出小节；新布局策略「小节分组」按组纵排、组间横排；两组成员间存在连线时组框之间自动出现汇总弧
  - 右键空白 就地新建章节（落在右键位置）/ 新建小节；「自动布局」按钮按当前策略整卷重排并落库
  - **卷细纲回归**：空白处单击 → 右侧面板显示卷统计 + 卷细纲编辑（600ms 防抖自动保存）
- **布局切换焦点追踪**：切换布局策略 / 自动布局后视图自动跟随——有选中章节就居中到它（保持缩放），否则自适应全图，图不再「跑不见」
- **小节连线**（V11 迁移 `group_edges`）：小节组框右侧中点新增连线圆点（悬停浮现）——拖到另一个小节或某个章节上即可建立「小节→小节 / 小节→章节」连线，七种边型、可拖弯（中点手柄）、点击编辑 / 删除；小节删除时其连线随之清理
- **布局模式重做与说明**：辐射改为真圆形环绕（核心章居中、其余在正圆上均匀分布，半径随章节数增大）；力导向加强（连线吸引 + 互斥 + 中心引力，结构由数据决定、结果确定不漂移）；切换提示与下拉悬停都会显示每种模式的一句话用途

## \[0.9.13] - 2026-09-08

### 新增（v0.9.13：分形画布与广义人物关系体系）

- **移除「全书排版」**：目录树「更多操作」菜单里的一键排版（去空白/删空行）按反馈删除，前后端命令一并移除；个别章节的格式整理仍可在编辑器内手动完成

- **边路由重构（类型分道）**：同对卷之间的多条连线不再按 id 均匀偏移，改为按边类型分道——顺序居中直线、因果上方微弧、分支上方大弧、汇合下方微弧、伏笔下方大弧、平行上方更大弧、闪回下方更大弧；同对同类型多条边再 ±0.4 微错。标签锚定从曲线中点（t=0.5）移到效应端（t=0.78），靠近终点箭头前、泳道间天然错开不再重叠；因果/分支/汇合/伏笔/平行/闪回标签默认显示（白底描边，长文截断）
- **连线弧度按边长放大 + 手动拖弯**：类型泳道基准弧高从固定 32px 改为「边长 × 12%（32~150px 夹紧）」——长跨度连线（如第一部→第三部的伏笔）不再是几乎笔直的平行束，弧度一眼可辨；每条连线的曲线中点在悬停时浮现小圆点手柄，**按住拖动即可弯曲线条**，松手落库（V9 迁移新增 `story_edges.bend`，归零即回到类型泳道基准弧度）；原地点击仍是编辑弹窗（拖动 >4px 判定为拖弯）
- **超量合并**：同对卷之间连线 >4 条时按优先级（顺序>因果>伏笔>分支>汇合）渲染前 4 条，多余合并为终点旁「+N」徽标，点击列出全部并可直接进入编辑
- **新增边类型**：5=平行叙事、6=闪回/插叙（建连线弹窗可选，图例同步）
- **L1 人物层**：卷节点信息增强——活跃伏笔徽标（🔴 N 伏笔）、核心人物行（本卷提及 Top4 小圆头像，颜色按角色定位）、关键事件标签（从卷细纲截取短语）；跨卷核心人物（≥2 卷出场）以圆形节点上画布，位置在首末出场卷之间；人物轨迹带（首出场卷到末出场卷的半透明纵向带，颜色=角色色，多人物垂直错开）；跨卷人物之间显示跨卷关系边；工具栏「👤 人物层」开关
- **人物视角模式**：点击跨卷人物节点，该人物的出场卷与直接关系人物保持高亮，其余元素淡出至 0.15，顶部提示条一键退出（点空白同样退出）
- **L2 卷内画布**（替换原章节卡片列表为默认视图）：章节方形节点（标题/字数/状态色边框/章纲摘要）+ 人物圆形节点（半径=14+log₂(提及数)×2.5，围绕出场最多章节堆叠）+ 三类边共存（剧情边=派生顺序线+章级伏笔点线；人物关系边=五类色线型独立偏移空间；出场边=浅灰细线默认隐藏）；章节位置由布局算法派生不存库；布局策略可切换：线性分支（默认）/ 辐射 / 力导向；工具栏筛选：人物层 / 关系边 / 出场边 / 布局策略；「列表」按钮保留原卡片列表（拖拽排序仍在列表完成）
- **L2 右侧详情面板**：选中章节 → 章纲/作者笔记（600ms 防抖保存）、本章出场人物 chips（点击切换到人物详情）、本章关联伏笔（活跃/已回收）、打开正文；选中人物 → 别名/角色/备注、本卷出场章节（点击跳章节）、关联人物关系列表（点击切换）
- **广义人物关系体系**：人物卡展开新增「人物关系」管理区——5 大类（1 血缘 / 2 情感 / 3 社会 / 4 阵营 / 5 叙事，各自定色定线型）+ 常用子类型下拉（父子/师徒/宿敌…）+ 自定义类型 + 单双向箭头 + 补充说明 + 作用范围（跨卷 / 仅指定卷）；关系边在画布上按大类着色，L2 显示跨卷+本卷关系，L1 只显示跨卷人物间的跨卷关系
- **V8 迁移**：新增 `character_relations` 表（人物删除级联清理，卷删除退化为跨卷关系）；出场统计命令 `list_character_volume_presence` / `list_volume_character_mentions`（character_mentions 按卷/按章聚合，纯渲染派生不落库）


## \[0.9.7] - 2026-09-07

### 改进（连线视觉体系 & 剧情线落地）

- **四类连线一眼可辨**：告别「全是灰黑细线」——每类独立 颜色 × 线型 × 粗细：顺序 = 中性灰细实线（背景性）、因果 = 强调色粗实线（核心推进）、分支/汇合 = 紫色长虚线、伏笔回收 = 青色点线（三主题各配紫/青语义色）；悬停与编辑中加粗高亮，失效伏笔淡化+更稀疏虚线

- **剧情线在画布上落地**（此前建了线毫无视觉反馈）：

  - 归属剧情线的连线整条染上剧情线颜色并加粗，中点带弧线色小徽标（悬停显示剧情线名）

  - 参与剧情线的卷节点卡片左侧显示弧线色条、右上角显示弧线色点

  - 建/编连线弹窗选择剧情线时，下拉旁实时预览该线颜色

  - 图例样式与画布连线同口径（颜色/线型一致）

- 一句话工作流：建剧情线 → 建连线时选它 → 连线和卷立刻染上该线颜色，多条剧情线并行时色彩即分层

## \[0.9.6] - 2026-09-07

### 修复（自动布局不再编造故事流）

- **停止编造假主线**：移除「同泳道按列相邻」的顺序边重建——此前互无关联的卷（包括完全没有任何连线的孤立卷）会被串上顺序箭头、排成一条假故事流，正是「自动布局只出一种错误线性方案」的根因。现在顺序边**只沿主线相邻重建**

- **孤立卷停泊**：与任何手工连线都不相连的卷，自动布局后排到主线下方的「未连接」泳道（按阶段序排开），不再挤进主线行——一眼可辨「这是已连好的故事流，这些是还没连的卷」

- 布局算法新增 2 个单元测试（孤立卷停泊不编造顺序边 / 同泳道互无关联的卷不被串连），后端 80 项测试全通过

## \[0.9.5] - 2026-09-07

### 修复（自动布局稳定性）

连续点击「自动布局」出现 3 种不同布局、其中一种是错误的线性排列——挖出三个 bug，逐一修复：

- **反馈循环**：自动布局会重建顺序边，而下一轮又把这些自己生成的顺序边当输入识别主线——等于吃自己的输出。现在布局只消费手工结构边（因果/分支/汇合/伏笔），顺序边纯为派生输出，**连点多少次结果都一致（幂等）**

- **随机性**：泳道挂靠 BFS 的队列从 HashMap 取种子，Rust HashMap 迭代顺序每次随机——同一份数据每次点击排布都不同。改为按（阶段序，id）确定性入队

- **分支卷被吞进主线**：兜底主线现在剔除「分支边目标」——从某卷分叉出去的卷作为子流程挂靠父卷右上，不再挤占主线行（此前即使画了分支边，兜底仍把全部卷排成一条线）

- 布局算法新增 3 个单元测试：顺序边反馈免疫（幂等）、纯线性数据稳定性、兜底主线剔除分支目标；后端 78 项测试全通过

## \[0.9.4] - 2026-09-07

### 变更（回归自由拖动）

- **故事地图节点改回自由拖动**：松手即停在原地、位置落库（归一化坐标，重启不丢），移除上一版的「松手弹回家位」弹性设计——固定式排版在边界情况下 bug 频出，自由摆放 + 持久化更直观可控

- 「自动布局」保留为一键整理（主线居中 / 支线浮上 / 暗线沉下 + 重建顺序边），整理后仍可自由拖动微调；确认弹窗文案同步更新

- 恢复 `move_story_node` 命令（拖拽落库 `volumes.map_x / map_y`）；无坐标的新卷继续用蛇形网格兜底

## \[0.9.3] - 2026-09-07

### 修复（自动布局观感两连修）

- **缩放太小**：布局坐标此前按节点数摊满世界宽度（5 个节点被拉出 350px+ 间距，fitView 只能缩到 \~74%，节点又小又稀疏）；改为 workflow 式**紧凑固定间距**（列距 280px / 泳道距 170px），以世界中心对称展开——少节点时缩放自然回到 90%\~140%，观感饱满

- **分支卷不再挤进主线行**：两个算法缺陷修复——

  - 无弧线时全部卷被误判为主线（`unwrap_or(0)` 吞掉了「无归属」），最长链识别从未生效；现在只认明确归属主线弧的卷，无主线弧时正确走「最长顺序/因果链」识别

  - BFS 泳道挂靠现在感知边类型：**分支边的子节点上浮一格**，出现在父卷右上方向（workflow 树状分叉），而不是被拉进主线行尾排队

- fitView 边距 60→40，布局后内容更充实

## \[0.9.2] - 2026-09-07

### 修复

- **故事地图自动布局重写**：原「按卷顺序蛇形网格」会把全部节点排成一条直线、丢失结构层次；现改为**泳道式分层布局**：

  - 主线（主线弧 kind=0，或最长 顺序/因果 链）居中泳道 0，X 按阶段序等距推进；

  - 支线弧逐条分列上方泳道（-1、-2、…），暗线弧逐条分列下方泳道（+1、+2、…）；

  - 无弧线卷沿连线挂靠最近邻泳道，孤立卷按节点类型（支线卷/番外）兜底；

  - 非主线卷从分叉点向右外扩，同泳道同列自动右移避让；

  - 顺序边按「主线相邻 + 同泳道按列相邻」整体重建；手工创建的 因果/分支/汇合/伏笔 连线不受影响；

  - 弧线归属纳入伏笔边（暗线伏笔的端点卷正确下沉），主线弧优先占据，避免支线伏笔把主线卷拉离主线泳道。

- **前端采用落库布局坐标**：故事地图家位优先读自动布局落库的分层坐标（此前前端忽略落库坐标、一律蛇形网格——这是「一条线」的直接原因）；点「自动布局」后即时刷新画面；拖拽弹性回弹保留，分层坐标即回弹家位。

- 布局核心抽为纯函数 `compute_layout` 并补 4 场景单元测试（全主线一条线 / 支线弧上浮 / 最长链主线 + 分叉外扩 / 暗线伏笔下沉），后端测试 75 项全通过。

- 版本号 0.9.1 → 0.9.2。

## \[0.9.0 / 0.9.1] - 2026-09

### 新增（可视化写小说模块）

- **故事地图（StoryMap）**：卷 = 故事阶段节点画布；自动布局、双击下钻、节点拖动、连线拖拽（顺序/因果/分支/汇合/伏笔回收）、右键菜单、选中节点编辑卷细纲。

- **卷内视图（VolumeView）**：卷细纲编辑器 + 本卷章节列表；章节可手动选定加入、跨卷移动、卷内排序、多选添加。

- **全书总览（OverviewView）**：剧情线泳道、伏笔总览（卷级/章级锚点、过滤、超期预警）、结构总览。

- **本章发展卡（InfoPanel）**：写作界面右侧面板新增「本章发展」——本章在所属卷内的前后章节、所属卷上下游连线、相关伏笔。

- **数据层**：V6/V7 迁移——`volumes.map_x/map_y`、`story_arcs` 剧情线、`story_edges` 连线（引用卷、伏笔支持章级锚点）；新增 `story_graph` 命令集。

- **入口整合**：侧边栏右键「卷大纲…」入口移除，卷细纲编辑统一收敛到可视化模块（画布节点抽屉 / 卷内视图）。

## \[0.4.0 \~ 0.8.0] - 累积版本

- 智能导入与自动拆章（docx/txt）、正文搜索、写作统计（今日字数/速度/时长）、自动备份与手动备份、回收站（软删除 7 天）。

- 人物/地点资料卡与正文提及识别、写作日报、专注模式、命名生成器、设置面板。

- 无边框窗口与首页仪表盘视觉升级（延续 v0.3.0）。

## \[0.3.0] - 2026-07

- 无边框窗口、首页仪表盘视觉升级。

## \[0.2.0] - 2026-06

- 版本号升级 0.1.0 → 0.2.0。

## \[0.1.0]

- 初始版本：本地小说创作与结构分析软件完整源码。

