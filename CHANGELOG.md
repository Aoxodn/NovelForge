# NovelForge 更新日志（CHANGELOG）

## [1.1.3] - 2026-09-09

### 重构

- **人物/地点弹窗与角色卡页职责分离**：解决两处功能重合
  - 人物/地点弹窗定位为「速览入口」：保留列表、搜索、五维排序、分类筛选、自定义拖拽、快速新建、地点管理、出场统计；点击人物直接跳转角色卡深度编辑页，不再内联展开编辑表单
  - 角色卡页承接深度编辑：新增出场热度走势（SVG 柱状图 + 断档预警）和人物关系管理（五类关系增删），与富文本人设、自定义字段、冲突检测、全书改名、弧光追踪统一在一处
  - 取名器保持独立简单工具，不扩展为素材库
- 新增共享组件 `HeatBars`、`RelationsSection`，供角色卡页复用

---

## [1.1.2] - 2026-09-09

### 修复

- **添加人物后删除无法再次添加**：角色节点右键新增「从画布移除」（L1 清 map_x/map_y、L2 删 char_volume_pos），不删卡即可拿掉，之后可随时重新添加；删卡路径同步清理乐观坐标并直接刷新列表，避免残留 id 留在选择器中

---

## [1.1.1] - 2026-09-09

### 修复

- **滚轮缩放失效**：故事地图的 SVG 在数据加载前不渲染，导致共享视口 hook 的滚轮监听器从未绑定；新增 `ready` 参数，SVG 挂载后自动绑定
- **角色无法创建在画板上**：人物层默认折叠后，右键「添加人物到图谱」放上去的角色不可见；放置时自动展开人物层

### 改进

- **人物 / 地点弹窗全面增强**：
  - 搜索框（名字 / 别名 / 阵营 / 标签）
  - 五种排序：按重要度 / 按名字 / 按出场次数 / 按角色定位 / 自定义拖拽
  - 分类筛选：角色定位（全部/主角/配角/反派/龙套）、存亡（全部/存活/死亡）、POV（全部/是POV/非POV）
  - 自定义拖拽排序落库（新增 `sort_order` 字段与 `reorder_characters` 命令）
  - 人物编辑表单补全群像字段：阵营、重要度、存亡、POV、标签
  - 列表项显示阵营 / POV / 已故 / 标签徽章，一眼区分

---

## [1.1.0] - 2026-09-09

本次为一次全项目工程审查后的系统性整改版本：修复若干确定性数据丢失 / 错乱问题，补齐性能与架构短板，并新增五个写作辅助功能。

### 数据安全（P0，发布前必修）

- **切章 / 退出不再丢正文**：保存失败会显式抛出并阻断切章；返回首页、关闭窗口前若保存失败，弹出「重试 / 导出未保存正文 / 强制退出」，不再无条件清空
- **串行保存队列**：保存过程中继续输入会标记 pending，本次写完立即补存最新快照，最后几个字不再滞留内存
- **统一可靠草稿**：角色卡、卷纲、章纲、信息面板的自动保存改为统一的 useDurableDraft（实体 ID + 修订号），切换 / 关闭 / 卸载前冲刷，失败显示常驻错误条而非 3 秒 Toast
- **目录刷新竞态**：保存后改用只读的当前连接局部刷新，不再重新 openProject；所有异步项目请求带请求序号，迟到响应不会再把旧项目盖回当前项目
- **备份恢复原子化**：恢复前先做 integrity_check，临时文件 + 改名交换，任一步失败自动回滚并重开连接，不再留下半写数据库或丢失连接

### 正确性与性能（P1）

- **画布数据约束**：修复同卷伏笔校验、跨卷章节加入小节等三处确定性约束错误，数据库增加触发器兜底，并补后端回归测试
- **自动布局不再压成一条线 / 重叠**：改为按列 / 泳道归一化布局，世界坐标不再被 clamp 到 0.02–0.98，长主线与多泳道都能保持层次，fitView 负责缩放
- **人物统计增量且不被旧结果覆盖**：只重算内容指纹过期的章节，写入前二次核对；回收站（软删）章节不再计入角色卡；删 / 恢复章节即时同步提及
- **全局唯一人名匹配**：改用全局 longest-leftmost 匹配器，「苏婉」「苏婉清」不再重复计数；单字名误判在全局层面排除；新增别名冲突检测
- **大项目性能**：统一匹配自动机按实体词指纹缓存，普通人设编辑不再触发全书重扫；百万字 / 数百角色级别保存进入毫秒级（附基准测试）
- **搜索**：前端加请求序号，迟到结果不再覆盖新查询
- **导入事务化**：分析缓存只在提交成功后消费，卷与章节写入同一事务，取消导入显式释放缓存，失败后无需重新分析

### 架构（P2）

- 后端抽出纯计算的 services/layout 布局层与 mention_index 匹配层，命令层只做编排
- 画布拖动用 requestAnimationFrame 合帧，拖动 transform 不再每帧触发 React 全量重绘；小节整体移动改为单事务批量命令
- 前端抽出共享的 useCanvasViewport 视口 hook（两画布统一缩放 / 平移 / fitBounds）
- AppError 结构化为 {code, message, retryable}，前端统一归一化，可重试错误可被识别
- 补齐前端测试框架（Vitest）与保存竞态、请求序号、错误归一化、段落切分等单测；Rust 侧 clippy 零告警

### 体验（UX）

- **专注模式错行修复**：根因是 textarea 滚动条使正文宽度比镜像层窄，逐行累积错位；现严格对齐两者宽度与排版度量
- **画布可发现性**：新增快捷键帮助面板、轴约定（横轴时间 / 纵轴剧情线）、面包屑；人物轨迹图层默认折叠，选中人物才展开，降低噪声
- **群像角色管理**：角色新增阵营 / 存亡 / 重要度 / POV / 标签 / 自定义字段；列表支持按这些维度搜索排序、保存筛选视图，并做虚拟滚动，数百角色也不卡顿；顶部提示别名冲突

### 新功能

- **全书安全重命名**：逐章预览命中（可排除对白、天然避开复合长名误替换），确认后单事务替换、自动为改动章节留存历史版本、旧名并入别名
- **连续性检查器**：纯本地规则检测「已死角色再现、活跃伏笔逾期未回收、角色长期断档」，问题进入可忽略 / 标记解决的确认队列，可一键跳到对应章节
- **场景级写作板**：章节之下拆场景，记录 POV / 时间 / 地点 / 目标 / 冲突 / 结果 / 目标字数，支持拖拽重排并一键合成为章节正文
- **人物弧光追踪**：按章为角色记录「欲望—选择—代价—状态变化」，可绑定章节，服务群像与多线成长
- **修订工作台**：超长句 / 相邻重复词 / 叠字标点的静态检查，以及带预览与自动留档的跨章查找替换

### 跨平台

- 打包目标从仅 Windows NSIS 扩展为 Windows / macOS / Linux，提供三套 Tauri 配置与 GitHub Actions 三平台构建矩阵
- macOS / Linux 使用原生标题栏（仅 Windows 保留自定义无边框控件）
- 项目名校验补齐 Windows 保留设备名（CON/PRN/AUX/NUL/COM*/LPT*）、尾随句点空格与跨平台非法字符

---

## [1.0.3-beta.1] - 2026-09-09

### 新功能

- **角色卡独立页面**：顶栏新增角色卡视图，左侧角色列表（支持搜索、按角色筛选、重要度排序），右侧富文本人设编辑区，800ms 自动保存；图谱中角色节点右键可直接跳转对应角色卡
- **卷拖拽排序**：左侧目录的卷标题行支持直接拖动排序，拖动时显示蓝色插入指示线，替代反复点击上移/下移按钮

### 修复

- **专注模式光标偏移**：点击第二行却高亮/输入到第一行的问题。根因是 textarea 默认 padding 导致镜像层文字错位，已归零 padding 并修正镜像同步时序
- **专注模式文字区域过小**：正文宽度从 720px 扩大到 980px，上下左右 padding 全面收窄，专注模式文字面积显著大于正常模式

### 改进

- **角色节点显示全名**：圆形节点缩小为纯角色色点，名字从"hover 才显示的暗淡小字"改为始终可见的 12px 加粗全名，同姓角色不再无法区分
- **角色数量管理**：角色卡列表支持搜索（名字/别名）、按角色筛选（主角/配角/反派/龙套）、按重要度+提及数排序，群像小说也能快速定位

---

## [1.0.2-beta.1] - 2026-09-08

### 改进

- **设置界面改为右侧边栏**：从弹窗改为从右侧滑入的侧边栏，调整字体/字号/行距时正文实时变化，所见即所得
- **首次安装默认护眼主题**：新用户首次打开自动使用护眼（sepia）主题，已设置过主题的用户不受影响

---

